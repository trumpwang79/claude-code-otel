apiVersion: 1

datasources:
  # VictoriaMetrics 兼容 Prometheus 查询接口，数据源类型仍然选 prometheus
  - name: VictoriaMetrics
    type: prometheus
    access: proxy
    url: http://<你的VictoriaMetrics地址>:8428
    isDefault: true

  - name: loki
    type: loki
    access: proxy
    url: http://loki:3100
